# Romantic music

Put your own background track here and name it `romantic.mp3`.
The player in `index.html` already points to `music/romantic.mp3`.
If the file is absent, `script.js` uses a light Web Audio piano/ambient fallback after the user starts the story.
