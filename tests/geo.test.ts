import {describe,it,expect} from 'vitest';import {latLonToVector3,vector3ToLatLon,haversine,bearing,interpolateGreatCircle} from '../src/utils/geo';import {searchPlaces} from '../src/geocoding/LocalGeocoder';
const tash={id:'a',name:'Tashkent',type:'city' as const,latitude:41.3111,longitude:69.2797},sam={id:'b',name:'Samarkand',type:'city' as const,latitude:39.6542,longitude:66.9597};
describe('geodesy',()=>{it('round-trips geographic coordinates',()=>{const p=vector3ToLatLon(latLonToVector3(41.3111,69.2797));expect(p.latitude).toBeCloseTo(41.3111,5);expect(p.longitude).toBeCloseTo(69.2797,5)});it('calculates distance and bearing',()=>{expect(haversine(tash,sam)).toBeGreaterThan(250);expect(bearing(tash,sam)).toBeGreaterThan(200)});it('interpolates a great circle',()=>{const x=interpolateGreatCircle(tash,sam,8);expect(x).toHaveLength(9);expect(x[0].latitude).toBeCloseTo(tash.latitude);expect(x.at(-1)?.longitude).toBeCloseTo(sam.longitude)})});describe('offline geocoder',()=>{for(const q of ['Tashkent','tashkent','ТАШКЕНТ','ташкент','Toshkent','TASHKENT','Tashknet'])it(`finds ${q}`,()=>expect(searchPlaces(q)[0]?.id).toBe('tashkent'));it('finds prefix and coordinate queries',()=>{expect(searchPlaces('Tash').some(x=>x.id==='tashkent')).toBe(true);expect(searchPlaces('41.3111, 69.2797')[0]?.type).toBe('coordinate')});it('does not invent unknown result',()=>expect(searchPlaces('zzzzzzzz')).toEqual([]))});
