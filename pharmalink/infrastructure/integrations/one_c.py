from pharmalink.infrastructure.integrations.custom import CustomERPAdapter


class OneCAdapter(CustomERPAdapter):
    """HTTP adapter shell for 1C deployments; endpoint mapping is configured per customer."""
