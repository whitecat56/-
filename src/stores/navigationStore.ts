import {create} from 'zustand';import type {GeoPlace,Route,SelectionMode,Settings} from '../types';
const defaultSettings:Settings={autoRotate:true,atmosphere:true,stars:true,routeAnimation:true,units:'metric',showHud:true,showFps:false,sound:false};
type State={currentLocation?:GeoPlace;startLocation?:GeoPlace;destination?:GeoPlace;selectedPlace?:GeoPlace;route?:Route;routeStatus:'idle'|'building'|'ready'|'error';selectionMode:SelectionMode;cameraTarget?:GeoPlace;error?:string;settings:Settings;recent:GeoPlace[];setStart:(p:GeoPlace)=>void;setDestination:(p:GeoPlace)=>void;setSelectionMode:(m:SelectionMode)=>void;setRoute:(r?:Route)=>void;setError:(x?:string)=>void;setSettings:(x:Partial<Settings>)=>void;swap:()=>void;clearRoute:()=>void}
export const useNavigationStore=create<State>((set,get)=>({routeStatus:'idle',selectionMode:null,settings:defaultSettings,recent:JSON.parse(localStorage.getItem('global-nav-recent')??'[]'),setStart:p=>set({startLocation:p,currentLocation:p,cameraTarget:p,selectionMode:null,error:undefined}),setDestination:p=>{const recent=[p,...get().recent.filter(x=>x.id!==p.id)].slice(0,12);localStorage.setItem('global-nav-recent',JSON.stringify(recent));set({destination:p,selectedPlace:p,cameraTarget:p,recent,selectionMode:null,error:undefined})},setSelectionMode:selectionMode=>set({selectionMode,error:undefined}),setRoute:route=>set({route,routeStatus:route?'ready':'idle'}),setError:error=>set({error,routeStatus:error?'error':get().routeStatus}),setSettings:x=>set({settings:{...get().settings,...x}}),swap:()=>set(s=>({startLocation:s.destination,destination:s.startLocation,cameraTarget:s.destination,route:undefined,routeStatus:'idle'})),clearRoute:()=>set({route:undefined,routeStatus:'idle'})}));
